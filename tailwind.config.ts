import type { Config } from 'tailwindcss'

export default {
    theme: {
    fontFamily: {
      'wanted': ['WantedSans', 'sans-serif'],
    },
    extend: {
        fontFamily: {
        'wanted': ['WantedSans', 'sans-serif'],
        },
    },
    },
    plugins: [],
} satisfies Config